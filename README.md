# ZenonERP

Modular, extensible ERP — multi-tenant SaaS and self-hosted standalone.

Local development setup (WAMP vhost, hosts entries, tenant creation): see
[docs/local-dev.md](docs/local-dev.md).

Deploying standalone on Plesk/cPanel (requirements, the `/install` wizard,
cron, updates, addon install, release packaging): see
[docs/standalone-deploy.md](docs/standalone-deploy.md).
